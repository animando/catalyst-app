{
  "name": "shared-utils",
  "composite": "true",
  "private": "true",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "prettier:check": "prettier . --check",
    "eslint:check": "eslint .",
    "lint": "yarn run prettier:check && yarn run eslint:check"
  }
}
