spaUrl: https://${ssm:catalyst-ui-distribution-domain}
kafkaClusterArn: ${ssm:catalyst-kafka-cluster-arn}
kafkaClusterId: ${ssm:catalyst-kafka-cluster-id}
privateSubnetId1: ${ssm:msk-subnet-az1-id}
privateSubnetId2: ${ssm:msk-subnet-az2-id}
privateSubnetId3: ${ssm:msk-subnet-az3-id}
kafkaBootstrapServer: ${ssm:catalyst-kafka-bootstrap-brokers-iam}
kafkaClusterName: ${ssm:catalyst-kafka-cluster-name}
lambdaSecurityGroup: ${ssm:lambda-security-group-id}
kafkaSslDisabled: false
userPoolId: ${ssm:user-pool-id}
userPoolClientId: ${ssm:user-pool-client-id}
userPoolDomain: ${ssm:user-pool-domain}
distributionId: ${ssm(eu-west-2):catalyst-ui-distribution-id}
userPoolClientSecretArn: ${ssm:user-pool-client-secret-arn}
