{
  "name": "catalyst",
  "version": "1.0.0",
  "license": "MIT",
  "private": "true",
  "workspaces": [
    "workspaces/backend"
  ],
  "scripts": {
    "deploy": "yarn workspace backend run serverless deploy",
    "destroy": "yarn workspace backend run serverless destroy"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  }
}
